package com.salonip.moviereviewsapp.utils

import java.util.*

// used this function to convert string into lower case
fun toLowerCase(text: String) : String {
    return text.lowercase(Locale.ROOT)
}